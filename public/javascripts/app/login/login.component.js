'use strict';

angular.
  module('login').
  component('login', {
    templateUrl: '/javascripts/app/login/login.template.html',
    controller: ['$http', function LoginController($http) {
      var self = this;
    }],
  });